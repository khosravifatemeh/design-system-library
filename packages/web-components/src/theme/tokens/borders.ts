import { TokenGroup } from "./base/token-group";

export const borderTokens = TokenGroup.create({
  name: "Border",
  properties: [],
  tokens: ["border-xs", "border-sm", "border-md", "border-lg", "border-xl"],
});
