import { TokenGroup } from "./base/token-group";

export const blurTokens = TokenGroup.create({
  name: "Blur",
  properties: [],
  tokens: [
    "blur-none",
    "blur-sm",
    "blur-md",
    "blur-lg",
    "blur-xl",
    "blur-2xl",
    "blur-3xl",
    "blur-4xl",
  ],
});
